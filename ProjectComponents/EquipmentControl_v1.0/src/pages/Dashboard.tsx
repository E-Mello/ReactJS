import styles from './Dashboard.module.css'

export function Dashboard() {
    return (
        <section className={styles.allContents}>
            <h1 className={styles.title}>Olá eu sou o Dashboard</h1>
        </section>
    );
}